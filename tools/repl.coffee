#!/usr/bin/env coffee
require('../boot')

repl = require("repl")
#require 'coffee-script/src/repl'

###
repl.start({
  prompt: "svu-hepler> ",
  input: process.stdin,
  output: process.stdout
});
###
